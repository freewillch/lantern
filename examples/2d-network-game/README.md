# Simple 2D Network Game

This example shows a minimal 2D multiplayer game built with Express and Socket.IO. Players control a square with the arrow keys, and their positions are synchronized across all connected clients.

## Running

```
npm install
npm start
```

Open <http://localhost:3000> in multiple browser tabs to see the synchronized movement.
